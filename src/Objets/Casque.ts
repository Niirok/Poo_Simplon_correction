class Casque extends Armure {

    emplacement : emplacement_Armure.Tete

    constructor(prix : number, poids : number, level: number, armor_val : number) { 
        super(prix, poids, level, armor_val);
        
    }
}